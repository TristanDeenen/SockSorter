#ifndef SOCK_COMPARER_H
#define SOCK_COMPARER_H
#include <Arduino.h>
/*
bool scanSock(SockScanner sockScanner);
void addSockID(SockScanner sockScanner, int n);
int compareSocks(SockScanner[] sockID);
*/
#endif
